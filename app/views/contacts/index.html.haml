#contacts-wrapper
  - i = 0
  - @contacts.each do |contacts|
    - i = i + 1
    %h2
      = contacts.name
    .left-block
      .item.address
        = raw contacts.address
      .item.item-title
        %span
          Прием заказов:
        %br
        %span
          Монтажный отдел:
        %br
        %span
          Email:
      .item.item-content
        %a{href: "tel: #{contacts.order_phone}"}
          = contacts.order_phone
        %br
        %a{href: "tel: #{contacts.montage_phone}"}
          = contacts.montage_phone
        %br
        %a{href: "mailto: #{contacts.montage_phone}"}
          = contacts.email
      - if i == @contacts.length
        .bid_button.with-text-warning.animate#bid_1
          %a{href: '/'}
          .bid_button_text
            ОСТАВИТЬ ЗАЯВКУ НА ЗАМЕР
    .right-block
      %div{id: contacts.map_id}
